import React, {Component} from 'react';

class PlaceMarkBalloon extends Component{
    // As for now, Web World Wind annotations are used instead of a custom balloon
}

export default PlaceMarkBalloon;